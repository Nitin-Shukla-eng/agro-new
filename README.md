# Agro Rangers — Plant a Tree Prototype

A Vite + React version of the uploaded prototype.

## Run locally

```bash
npm install
npm run dev
```

## Build for deployment

```bash
npm run build
```

The production output is generated in `dist/`.
